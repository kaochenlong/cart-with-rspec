<h1>購物車</h1>

<% if current_cart.empty? %>
  目前購物車無內容
<% else %>
  <ul>
  <% current_cart.items.each do |item| %>
    <li><%= item.product.title %> (<%= item.product.price %> x <%= item.quantity %>) = <%= item.total_price %></li>
  <% end %>
  </ul>
  <%= current_cart.total_price %>
<% end %>
